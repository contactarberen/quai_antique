{% extends 'base.html.twig' %}

{% block title %}Editer les Plats{% endblock %}

{% block body %}
    <h1>Editer les Plats</h1>

    {{ include('plat/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plat_index') }}">retour à la liste</a>

    {{ include('plat/_delete_form.html.twig') }}
{% endblock %}
