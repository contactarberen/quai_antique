{% extends 'base.html.twig' %}

{% block title %}Plat{% endblock %}

{% block body %}
    <h1>Plat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ plat.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ plat.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ plat.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plat_index') }}">Retour à la liste</a>

    <a href="{{ path('app_plat_edit', {'id': plat.id}) }}">Editer</a>

    {{ include('plat/_delete_form.html.twig') }}
{% endblock %}
