<form method="post" action="{{ path('app_formule_delete', {'id': formule.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule.id) }}">
    <button class="btn">Supprimer</button>
</form>
