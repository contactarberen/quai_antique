{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %} 
    <div class="container-fluid ">
        <div class="row mb-5">
            <img src="{{ asset('img/photos_entete/photo_ent_1.png') }}" alt="test" class="img-responsive">
        </div>
        <div class="row row-cols-1 row-cols-sm-2">
            <div class="photo text-start">
                <img src={{ asset('uploads/' ~ photos_nom[0]) }} class="w-100 p-3" alt={{photos_nom[0]}}>
                {#QA-66:titre lors du survol #}
                <div class="overlay">
                    <div class="overlay-content">
                        <h4 class="titre">{{photos_titre[0]}}</h4>
                    </div>
                </div>
            </div>
            <div class="col text-center my-auto">
                <h2>Restaurant Gastronomique à Chambéry</h2>
                <p>Restaurant 3 étoiles Michelin situé en Savoie, la table gastronomique du Chef Arnaud Michant 
                est d'une extrême sensibilité.</p> 
                <p>Cet amoureux des "produits bruts" sait faire résonner les matières et les saveurs.</p> 
            </div>

            <div class="col text-start my-auto">
                <h2>Expériences culinaires inoubliables</h2>
                <p>Toute l'équipe de cuisine apporte un soin tout particulier au choix des produits.</p> 
                <p>C'est un coin de paradis dans lequel vous pourrez découvrir l'une des meilleures tables de Savoie.</p> 
            </div>
            <div class="photo col text-end">
                <img src={{ asset('uploads/' ~ photos_nom[1]) }} class="w-100 p-3" alt={{photos_nom[1]}}>
                {#QA-66:titre lors du survol #}
                <div class="overlay">
                    <div class="overlay-content">
                        <h4 class="titre">{{photos_titre[1]}}</h4>
                     </div>
                </div>
            </div>
            
            <div class="photo col text-left">
                <img src={{ asset('uploads/' ~ photos_nom[2]) }} class="w-100 p-3" alt={{photos_nom[2]}}>
                {#QA-66:titre lors du survol #}
                <div class="overlay">
                    <div class="overlay-content">
                        <h4 class="titre">{{photos_titre[2]}}</h4>
                     </div>
                </div>
            </div>
            <div class="col text-center my-auto">
                <h2>Qualité du goût et de la présentation</h2> 
                <p>Décor unique, ambiance chaleureuse, cuisine sensible, la table de Arnaud Michant vous convie 
                à une expérience unique dans un lieu à part.</p>
            </div>
            <div class="photo col-12 text-left">
                <img src={{ asset('uploads/' ~ photos_nom[3]) }} class="w-100 p-3" alt={{photos_nom[3]}}>
                {#QA-66:titre lors du survol #}
                <div class="overlay">
                    <div class="overlay-content">
                        <h4 class="titre">{{photos_titre[3]}}</h4>
                     </div>
                </div>
            </div>
            <div class="col d-flex flex-row-reverse">
                <a href="/reservation/new" class="btnPrincipal btn btn-info ms-5 mt-4 mb-4">Réserver</a>
            </div>
            
        </div>
    </div>
{% endblock %}

{% block footer %} 
    <div class="bas_de_page container-fluid pt-5 text-center">
        <div class="row justify-content-evenly">
            <div class="horaires col-12 col-sm-6">
                <h2>Nos horaires d'ouverture</h2>
                    <table class="table">
                        <tbody>
                            {% set horaire_prec = "" %}
                            {% for horaire in horaires %}
                                <tr>
                                    {% if horaire.jour != horaire_prec %}
                                        <td>{{ horaire.jour }}</td>
                                    {% else %}
                                        <td></td>
                                    {% endif %}

                                    {% set horaire_deb = horaire.debut ? horaire.debut|date('H:i') : '' %}
                                    {% set horaire_fin = horaire.fin ? horaire.fin|date('H:i') : '' %}
                                    
                                    {% if horaire_deb == "00:00" and horaire_fin == "00:00" %}
                                        <td colspan="2">Fermé</td>
                                    {% else %}
                                        <td>{{ horaire_deb }}</td>
                                        <td>{{ horaire_fin }}</td>
                                    {% endif %}
                                    
                                    {% set horaire_prec = horaire.jour %}
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3">aucune entrée trouvée</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
            </div>
            <div class="infos col-12 col-sm-6">
                <p>Plans</p>
                <img src="{{ asset('img/maps.png') }}" alt="" class="img-fluid">
                <div class="row p-3">
                    <div class="col"><img src="{{ asset('img/icones_res_soc/facebook.png') }}" class="img-thumbnail"  width="30" height="30" lt="..."></div>
                    <div class="col"><img src="{{ asset('img/icones_res_soc/instagram.png') }}" class="img-thumbnail"  width="30" height="30" alt="..."></div>
                    <div class="col"><img src="{{ asset('img/icones_res_soc/twitter.png') }}" class="img-thumbnail"  width="30" height="30" alt="..."></div>
                    <div class="col"><img src="{{ asset('img/icones_res_soc/whatsapp.png') }}" class="img-thumbnail"  width="30" height="30" alt="..."></div>
                    <div class="col d-flex align-items-center">A propos</div> 
                    <div class="col d-flex align-items-center">Nous contacter</div>
                </div>
                <p>2023 - yasar production pour studi © Tous droits réservés</p>
            </div>
        </div>
    </div>
{% endblock %}
