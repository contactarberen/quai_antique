{% extends 'base.html.twig' %}

{% block title %}Editer les Photos{% endblock %}

{% block body %}
    <h1>Editer Photos</h1>

    {{ include('photo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_photo_index') }}">Retour à la liste</a>

    {{ include('photo/_delete_form.html.twig') }}
{% endblock %}
