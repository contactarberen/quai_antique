<form method="post" action="{{ path('app_horaire_delete', {'id': horaire.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaire.id) }}">
    <button class="btnPrincipal btn">Supprimer</button>
</form>
